# Backend Environment Variables
PORT=9000
DATABASE_URL=jdbc:mysql://localhost:3306/travelvibe
MYSQL_USER=root
MYSQL_PASSWORD=changeme
SHOW_SQL=false

# Frontend Build Variables (used by Vite)
VITE_API_BASE_URL=http://localhost:9000

# Notes:
# 1. Copy this file to .env and adjust values for local development.
# 2. In GitHub Actions, BACKEND_URL secret will override VITE_API_BASE_URL for production build.
# 3. Do not commit your real passwords; keep them in secrets or local .env only.