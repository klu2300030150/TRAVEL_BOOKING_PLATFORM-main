spring.application.name=TravelSathi
# Server port - Railway will override with PORT env variable
server.port=${PORT:9000}

# Database Configuration - Railway MySQL
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/travelvibe}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:Sreekar@8297}

# JPA/Hibernate - MySQL dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# CORS - Allow GitHub Pages frontend
spring.web.cors.allowed-origins=https://klu2300030150.github.io,http://localhost:3000
