-- Flyway Migration: Initial schema
-- Creates example tables if not already managed by JPA.
-- Adjust to match your entities or leave empty to let JPA create then baseline.

-- Example placeholder (commented out). Uncomment and modify when ready.
-- CREATE TABLE sample_table (
--   id BIGINT PRIMARY KEY AUTO_INCREMENT,
--   name VARCHAR(255) NOT NULL,
--   created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
--);
